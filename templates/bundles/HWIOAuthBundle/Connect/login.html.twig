{% extends '@!HWIOAuth/Connect/login.html.twig' %}

{% block hwi_oauth_content %}
    <h2>Wählen Sie einen LoginProvider um sich zu authentifizieren.</h2>
    {% if error is defined and error %}
        <span>{{ error }}</span>
    {% endif %}
    {% for owner in hwi_oauth_resource_owners() %}
    <a class="se-login-provider-link" href="{{ hwi_oauth_login_url(owner) }}">{{ owner | trans({}, 'HWIOAuthBundle') }}</a> <br />
    {% endfor %}
{% endblock hwi_oauth_content %}
